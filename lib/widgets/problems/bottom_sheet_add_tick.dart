import 'package:flutter/cupertino.dart';

class BottomSheetAddTick extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Tää on bottom sheet"),
    );
  }
}
